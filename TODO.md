# TODO

- [x] Document breaking changes and what the new package should have
- [x] Review adapter API consistency and add simplification regression tests
- [x] Injection helpers require an injection context; use `runInInjectionContext` when needed
- [ ] Create the PR for the `@tanstack/angular-query` packages
- [ ] Review how docs work. Can we support both experimental and the ner version at the same time?
- [ ] Talk to Analog mantainers to do a heads up of the breaking changes
- [ ] Talk to OpenNG mantainers to alight and see if instead of maintaning the extra fork we could provide a migration guide
